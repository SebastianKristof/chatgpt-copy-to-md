{
  "name": "chatgpt-copy-to-md",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "zip": "node build.js && (cd dist && zip -r ../dist.zip .)"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
